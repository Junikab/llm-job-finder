# Copy to .env at repo root
OPENAI_API_KEY=
PORT=5174
SCRAPER_HEADLESS=true
MAX_JOBS=40
MAX_PAGES=3
JORA_REGION=au

## Scoring mode: random | heuristic | llm
# default is random; llm currently falls back to heuristic until wired
SCORE_MODE=random
